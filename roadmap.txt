1. **Fase 2 Guards __main__** — Revisao de API/documentacao dos 15 modulos-biblioteca (core/base, core/utils, core/stealth, data/loader, web/secondorder, mobile/apk_*, ipa_*, oauth2_flows, _common) apos os 33 guards da fase 1.
2. **Workspace de Scan** — [IMPLEMENTADO] workspace_timestamp() + workspace_path() em utils.py, --output-dir em add_base_args (Group B: dois arquivos com -o; Group A: -o vence) + injecao central em run_main_loop (legacy com --output-dir ganham workspace automaticamente). 9+3 testes. Nota: lote legacy pendente (batch, reconall mantem --output-dir proprio).
3. **Padronizar Modulos Legacy** — Unificar os 9 modulos nao-BaseScanner com barra manual (configfiledetect, backupfiledetect, vcsleak, googledorking, emailbreachcheck, graphqlplayground, openapidiscovery, sourcemapdiscovery, dirscanner) para add_common_args/BaseScanner: --quiet, --json, --theme consistentes.
4. **Menu Dinamico** — Gerar menu do mytools via importlib.metadata (entry points), eliminando as 74 launch_* hardcoded em core/main.py (2419 linhas) e expondo os ~39 comandos fora do menu. Base para o plugin system.
5. **HTML Report Generator** — Template HTML com findings, severidade, evidencias, timeline. Diff entre scans. Requer sistema de template + engine de comparacao. (usando jinja2)
6. **Headless Browser** — Checagens de DOM real para domclobbering, mxss, cookieboundary (hoje heuristica HTTP).
