1. **Fase 2 Guards __main__** — Revisao de API/documentacao dos 15 modulos-biblioteca (core/base, core/utils, core/stealth, data/loader, web/secondorder, mobile/apk_*, ipa_*, oauth2_flows, _common) apos os 33 guards da fase 1.
2. **Workspace de Scan** — [IMPLEMENTADO] workspace_timestamp() + workspace_path() em utils.py, --output-dir em add_base_args (Group B: dois arquivos com -o; Group A: -o vence) + injecao central em run_main_loop (legacy com --output-dir ganham workspace automaticamente). 9+3 testes. Nota: lote legacy pendente (batch, reconall mantem --output-dir proprio).
3. **Padronizar Modulos Legacy** — [IMPLEMENTADO] add_common_args (base+http+stealth) nos 9 modulos com barra manual (configfiledetect, backupfiledetect, vcsleak, googledorking, emailbreachcheck, graphqlplayground, openapidiscovery, sourcemapdiscovery, dirscanner); --json ativo (saida por alvo + agregada, padrao vcsleak); --output-dir adicionado a googledorking/emailbreachcheck (demais ja tinham). ~25 novos testes (TestCommonFlags + TestJsonOutput + TestOutputDir).
4. **Menu Dinamico** — [IMPLEMENTADO] main.py reescrito (2419 -> 579 linhas): menu gerado via importlib.metadata entry points (112 ferramentas, 10 categorias), navegacao 2 niveis (categorias -> modulos paginados 10/pagina com n/p), busca por modulo/script/alias, dispatch via module.main() com import lazy, sair de tool volta as categorias. 29 testes em test_main.py. Base para o plugin system.
5. **HTML Report Generator** — [IMPLEMENTADO] novo modulo core/report.py + entry point mytools-report: le scans JSON (workspace -d/--dir ou arquivos -f/--file), normaliza findings por formato (Group B attempts / attackaudit findings / legacy listas), deriva severidade (cores HEX), renderiza HTML com jinja2 (cards por severidade, timeline multi-scan, tabelas de diff). Diff entre scans do mesmo host por fingerprint/multiset (added/removed/unchanged), comparando os 2 scans mais recentes. Flag --tool opcional. Dep nova: jinja2>=3.1.6. 25 testes em test_report.py. Menu: entra como HTML Report (core, 113 tools).
6. **Headless Browser** — Checagens de DOM real para domclobbering, mxss, cookieboundary (hoje heuristica HTTP).
