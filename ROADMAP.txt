# MyTools Roadmap
# Itens pendentes e futuro identificados na auditoria de otimizacoes

## 1. asyncio.run() no shell interativo (resolvido)
# Adicionado safe_asyncio_run() em utils.py que detecta event loop ativo
# e executa coroutine em thread separada quando necessario.
# Afeta: dirscanner.py, webrecon.py, attackaudit.py.

## 2. Modo --all / wrapper script (resolvido)
# Criado reconall.py (mytools-reconall) que executa portscanner, dirscanner,
# webrecon, attackaudit, dnstransfer e subenum contra um alvo.
# Suporta --skip, --deep, --cve, --test-vulns, --dry-run, -o (output dir).
